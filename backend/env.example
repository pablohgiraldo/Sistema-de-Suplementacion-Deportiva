# MongoDB Connection String
# Obtén esta URI desde MongoDB Atlas: Connect > Connect your application
MONGODB_URI=mongodb+srv://usuario:password@cluster0.abc123.mongodb.net/supergains?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=tu_secreto_jwt_muy_seguro_aqui
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# CORS Origin (URL de tu frontend)
CORS_ORIGIN=https://tu-frontend.vercel.app

# Node Environment
NODE_ENV=production

# Port
PORT=10000
